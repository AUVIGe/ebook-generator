# 📚 一键电子书生成器 - 本地运行版

## 方式一：Windows / Mac 电脑（最简单）

### 步骤 1：确认已安装 Python 3
- **Mac**：打开"终端"，输入 `python3 --version`，看到版本号即可（Mac 自带）
- **Windows**：到 https://www.python.org/downloads/ 下载安装（**安装时勾选 "Add to PATH"**）

### 步骤 2：启动网站
1. 把整个 `ebook-generator` 文件夹放到桌面（或者其他任何位置）
2. 打开终端/命令行
3. 进入文件夹：
   - Mac：`cd ~/Desktop/ebook-generator`
   - Windows：`cd C:\Users\你的用户名\Desktop\ebook-generator`
4. 启动服务：
   ```bash
   python3 -m http.server 8000
   ```
   Windows 用 `python -m http.server 8000`
5. 看到 `Serving HTTP on 0.0.0.0 port 8000` 即可
6. 浏览器打开：**http://localhost:8000**

### 步骤 3：手机访问（同一个 WiFi）
- 查看电脑 IP：
  - Mac：终端输入 `ifconfig | grep "inet "`（找 `192.168.x.x` 那个）
  - Windows：命令行输入 `ipconfig`（找 IPv4 地址）
- 手机浏览器打开 `http://电脑IP:8000`
- 就能用手机访问了 ✅

### 关闭
- 终端里按 `Ctrl + C` 即可

---

## 方式二：没有 Python（用 Node.js）

### 步骤 1：安装 Node.js
到 https://nodejs.org/ 下载 LTS 版本安装

### 步骤 2：启动
1. 进入 `ebook-generator` 文件夹
2. 终端运行：
   ```bash
   npx serve -p 8000
   ```
3. 浏览器打开 **http://localhost:8000**

---

## 方式三：双击直接打开（最最简单，但有坑）

**Mac**：
1. 找到 `ebook-generator` 文件夹里的 `index.html`
2. 右键 → "打开方式" → 选浏览器
3. ❗ **注意**：这种方式 PDF 解析可能不工作（浏览器安全限制），但 Word 和其他功能正常

**Windows**：
1. 双击 `index.html` 用浏览器打开
2. ❗ 同上，PDF 可能不工作

**建议用方式一**，最稳。

---

## 📁 文件夹结构

```
ebook-generator/
├── index.html          ← 主页面
├── script.js           ← 核心逻辑
├── style.css           ← 样式
├── lib/                ← 第三方库（不要删）
│   ├── mammoth.min.js
│   ├── pdf.min.js
│   ├── pdf.worker.min.js
│   ├── qrcode.min.js
│   └── lz-string.min.js
├── 启动教程.md         ← 本文件
└── 双击启动.command     ← Mac 一键启动（方式一）
```

---

## 🆘 遇到问题？

**问：启动后浏览器打不开？**
答：检查 8000 端口是否被占用，换一个端口试试：
```bash
python3 -m http.server 8888
```
然后访问 `http://localhost:8888`

**问：手机上访问电脑网站？**
答：手机和电脑必须在同一个 WiFi 下，然后用电脑 IP + 8000 端口访问。

**问：生成的海报下载不了？**
答：现代浏览器会自动下载到默认下载文件夹。如果没反应，检查浏览器是否拦截下载。

---

## 🎯 快速功能一览
1. **上传文档** - 拖拽 / 点击上传 PDF 或 Word
2. **选封面** - AI 自动绘制 / 上传图片
3. **一键生成** - 自动排版 + 生成二维码 + 合成海报
4. **下载海报** - 720×1280 PNG，可分享到任何地方
5. **扫码阅读** - 朋友扫码就能在手机上阅读

**字数无限制**，随便传。30万字以内链接最稳定。
