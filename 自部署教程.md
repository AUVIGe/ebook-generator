# 🚀 一键电子书 - 自部署完整教程

## 🔴 核心问题
微信对 `*.space.minimaxi.com` 子域名有限制（腾讯安全策略），扫码后会被拦截。
**根本解法**：把网站部署到你自己可信任的域名。

---

## 🚀 方案 A：一键部署到 Vercel（推荐，5分钟，免费）

Vercel 是 Next.js 团队的免费静态托管，国内访问速度好。

### 步骤：
1. 注册 [vercel.com](https://vercel.com)（GitHub 登录即可）
2. 安装 Vercel CLI：
   ```bash
   npm i -g vercel
   ```
3. 解压本 zip，进入 `ebook-generator` 文件夹：
   ```bash
   cd ebook-generator
   vercel
   ```
4. 按提示操作：
   - `Set up and deploy?` → Y
   - `Which scope?` → 选你的账号
   - `Link to existing project?` → N
   - `What's your project's name?` → ebook-generator（任意）
   - `In which directory is your code located?` → ./
   - `Want to modify these settings?` → N
5. 等待部署完成，会给你一个 URL：`https://ebook-generator-xxx.vercel.app`
6. （可选）绑定自己的域名：Project Settings → Domains → 添加

部署后用这个 URL 生成海报 → 微信扫码即可直接跳转！

---

## 🌟 方案 B：部署到腾讯云开发（国内访问最快）

适合想要"微信内快速访问"的场景。

1. 开通 [腾讯云开发静态网站托管](https://console.cloud.tencent.com/tcb/hosting)
2. 创建环境，选"静态网站托管"
3. 点击"上传文件"，把 `ebook-generator` 文件夹里所有内容上传
4. 获得域名 `xxx-xxxx.tcloudbaseapp.com` 或绑定自己的域名

---

## 🐙 方案 C：部署到 GitHub Pages（适合有 GitHub 账号）

1. 在 GitHub 创建新仓库 `ebook-generator`
2. 把所有文件 push 上去
3. Settings → Pages → Source: main branch
4. 获得 `https://你的用户名.github.io/ebook-generator`

---

## 🇨🇳 方案 D：部署到 Gitee Pages（国内访问快）

1. 在 Gitee 创建仓库
2. 上传所有文件
3. 服务 → Gitee Pages → 启动

---

## 📱 部署后生成海报的流程

1. 访问你部署好的网站
2. 上传文档 → 选封面 → 一键生成
3. 下载海报（PNG，720×1280）
4. 微信扫描海报 QR
5. 直接在微信内置浏览器打开，**这次不会被拦截**（因为是你自己的域名）

---

## 💡 推荐：用自己域名

如果你有自己的域名（已备案），强烈建议部署到自己的域名下。
比如 `ebook.your-domain.com`，微信完全信任，扫码秒开。

---

## 🔧 高级：API key 配置

如果想用更高质量的 QR 服务（如草料二维码），可以：

1. 注册 [草料二维码](https://cli.im) 账号
2. 申请 API key
3. 打开 `script.js`，找到 `QR_APIS` 数组，在第一个位置加：
   ```js
   (text, size) => `https://api.cli.im/QRCode/Create/v2?key=你的KEY&text=${encodeURIComponent(text)}&mhid=你的模板ID&size=${size}`
   ```

---

## 📂 文件清单

```
ebook-generator/
├── index.html          # 主页面
├── script.js           # 核心逻辑（含 QR 远程 API + 本地 fallback）
├── style.css           # 样式
├── lib/                # 第三方库（本地，离线可用）
├── 双击启动.command    # Mac 启动脚本
├── 双击启动.bat        # Windows 启动脚本
├── 启动教程.md         # 本地使用教程
└── 自部署教程.md       # 本文件
```

---

## ⚠️ 关于远程 QR 服务

当前实现：远程 quickchart.io + 本地 fallback
- 优点：远程 QR 兼容性更好
- 缺点：依赖 quickchart.io（被墙时自动 fallback）
- 已测试：quickchart.io 在 sandbox 可访问，国内访问可能略慢但能用

如要**完全离线**：可以编辑 `script.js`，注释掉 `loadRemoteQR` 那段，让 `generateQRCode` 直接调用 `generateQRLocal`。

---

## 💬 需要帮助？

如果某个步骤卡住，告诉我具体哪一步，我帮你看。
